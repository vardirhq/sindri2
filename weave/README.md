# Weave

Experimental declarative UI styling language for Sindri.

This workspace is intentionally independent of Sindri. It owns parsing, selectors, declarations, responsive conditions, diagnostics, and language tooling. Sindri integration belongs exclusively in `crates/sindri-weave` in the parent workspace.
