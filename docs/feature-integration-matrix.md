# Feature integration matrix

Sindri features cross four product surfaces. A runtime type alone is not an
authorable engine feature, an editor control is not useful when a game cannot
run what it writes, and a Decay binding is not proven until gameplay uses it.
This table keeps those counterparts visible in one place.

Update this file in the same change that moves any cell. `docs/capabilities.md`
remains the detailed evidence for what works; this is the cross-surface index
that makes missing integrations difficult to overlook.

Status terms are deliberately plain:

- **Ready** — implemented and exercised on that surface.
- **Partial** — a useful slice works, but the named gap still matters.
- **Missing** — planned or required, but unavailable.
- **N/A** — the surface does not need a counterpart for the feature.

| Feature | Engine/runtime | Editor authoring | Decay | Gather proof | Next integration gap |
| --- | --- | --- | --- | --- | --- |
| Entities and hierarchy | **Ready** — safe handles, parents, recursive destruction, commands | **Ready** — create/delete, folding, drag reparent | **Ready** — find, inspect, reference, spawn, reparent, despawn | **Ready** — scripts find and despawn orb entities | Undo entries for script writes |
| Prefabs | **Ready** — versioned single-root scene fragment, validated, canonical, spawned into a world | **Missing** — nothing makes one from a selection, and a `Prefab` field draws as text rather than an asset picker | **Ready** — typed `World.spawn`, overrides through the reference, `set_property` before first callback, numeric property reads | **N/A** — Gather shows what already worked; Orbital Last Stand exercises nested projectile/effect prefabs and per-instance damage | Editor authoring, then instance links so editing a prefab updates instances |
| Transform | **Ready** — shared 2D/3D transform and checked Z lock | **Ready** — position/rotation/scale inspector plus command-backed Scene gizmos, snapping, and Z-lock-safe movement | **Partial** — typed position/scale paths; no structured vector value | **Ready** — movement is scripted | Structured vector and rotation access in Decay |
| Camera | **Ready** — perspective and orthographic, pixel snapping | **Partial** — view controls and authored Game view; no gizmo | **Partial** — `Viewport.aspect` exposes screen shape, but scripts cannot inspect or control cameras | **Ready** — authored camera renders the game; Orbital Last Stand combines its authored framing with viewport aspect for combat visibility | Decide whether gameplay needs direct typed camera access |
| Sprites and sheets | **Ready** — world sprites, sheets, UVs, layers, blending | **Ready** — components, slicer, texture picker, picking | **Partial** — typed sprite asset and visibility paths only | **Ready** — floor props, player, orbs | Generate the wider component surface from schemas |
| UI | **Ready** — anchored image and text, fill bars, buttons, row/column layout, safe area, and `ScreenUi` laying it all out and hit-testing the pointer | **Ready** — listed apart from world objects, created directly, anchors chosen from the engine's list; no on-canvas element handles | **Ready** — `Ui.set_text`/`set_number`/`set_numbers`/`set_fill`, `is_hovered`/`is_pressed`/`is_held`, `Pointer.over_ui`, and `this.ui_image` tint and layer | **Ready** — banner, score lamps, title | On-canvas element handles; a scroll region and web accessibility labels when something needs them |
| Sprite animation | **Ready** — clips, timing, loop state | **Ready** — clip/frame authoring and preview | **Missing** — cannot select or control clips | **Ready** — player animation advances in play | Expose typed play/stop/clip paths to Decay |
| Tilemaps | **Ready** — orthogonal/isometric rendering and cell lookup | **Ready** — resize, palette, paint/erase | **Missing** — no tile queries or writes | **Ready** — floor is one tilemap | Define typed read/write access without exposing storage details |
| Text | **Ready** for screen text — `sindri.ui.text` with `{}` slots a script fills, and project fonts; no world text or rich layout | **Ready** for current component | **Ready** — `Ui.set_text` swaps the words, `set_number`/`set_numbers` fill the slots | **Partial** — static title; score and win state remain UI images | World-space text, if something needs it |
| Effects | **Ready** — a measured pooled fleck path that batches with ordinary sprites; see `docs/effect-scaling.md` | **Partial** — `sindri.effect.burst` is registered and editable through the generic inspector; no preview and no on-canvas burst | **Ready** — `Effects.burst`/`burst_at`/`live` | **N/A** — Gather throws nothing; `games/orbital-last-stand` is where this gets its gameplay proof | An instanced primitive renderer and custom materials when something needs them; the per-frame payload re-parse the measurement exposed |
| Persistence | **Ready** — versioned flat key/value documents, atomic file replacement, browser storage, memory and damaged backends, and states gameplay can act on | **Partial** — editor Play keeps a save in memory so persistence can be play-tested; nothing is written to disk and there is no save inspector | **Ready** — `Save.number`/`flag`/`has`/`clear` and `is_new`/`is_damaged`/`is_from_newer` | **N/A** — Gather has nothing to remember; `games/orbital-last-stand` is where this gets its gameplay proof | A save inspector in the editor, and migration steps when a version bump first needs one |
| Randomness | **Ready** — a written-out PCG stream, replayable from a seed on every host, with unbiased bounded draws and no platform entropy | **Ready** — editor Play reseeds on every fresh start, so a run repeats | **Ready** — `Random.value`/`range`/`int`/`pick`/`seed` | **N/A** — Gather is deterministic by design; `games/orbital-last-stand` is where this gets its gameplay proof | Per-system streams if one shared stream proves too coupled |
| Assets | **Ready** — IDs, manifests, async loading, texture/font/script reload | **Partial** — browse/select/slice; cannot create, delete, or import watched files | **Partial** — string-like asset fields, no safe asset value | **Ready** — project-owned assets on native and web | Safe typed asset handles and project import workflow |
| Input | **Ready** — keyboard, mouse, and touch behind the platform boundary, with a unified pointer and a bounded set of fingers | **Ready** — Play routes keyboard, buttons and touch, with positions in the Game view's own pixels and a pointer outside it reported as gone | **Ready** — `Pointer` position, presence and button edges; `Touch` count and per-finger positions | **Ready** — arrow-key movement; Gather has no pointer control to add | A gamepad, and a scroll surface for Decay |
| Tags and entity queries | **Ready** — `sindri.tags` is world data in `sindri-core`, and a bounded active-only query answers in deterministic world order | **Ready** — registered with an empty default and editable through the generic component inspector | **Ready** — `World.with_tag` answers with `Array<Entity>` and `World.has_tag` inspects one collision handle | **N/A** — Gather names its five orbs and needs no group; Orbital Last Stand uses both query shapes at combat density | Query by more than one tag |
| Decay scripts and exports | **Ready** — typed host, compile/run/reload, safe entity references, prefab references gathered from declared export types | **Partial** — component and `@export` fields; source cannot be created or edited, and a `Prefab` field has no picker | **Partial** — useful gameplay subset with collections; no LSP, formatter, or debugger | **Ready** — all gameplay rules are Decay | Pointer input, then collision access, then source tooling |
| Play mode | **Ready** — the shipped fixed-step loop, with input edges consumed exactly once | **Ready** — play/pause/stop, snapshot restore, and single-step | **Ready** for current script lifecycle | **Ready** | A profiler view of where a fixed step goes |
| Grid and isometric gameplay | **Partial** — typed geometry, authored footprints/walls, derived world occupancy, pathfinding, and world/screen Y orientation; tilemaps remain the shared projection authority | **Partial** — tile painting plus dedicated wall and footprint/occupant inspector authoring; no viewport wall painting or height tools | **Ready** — typed continuous position/read/place plus pathfinding calls through an explicit tilemap entity | **Ready** — movement, bounds, collection, and Wisp pathfinding use the floor's logical isometric coordinates | Richer viewport navigation painting and height authoring |
| Pathfinding and occupancy | **Ready** — deterministic walls, A*, whole-footprint paths, authored components, and a validated world adapter | **Ready** — tilemap walls and occupant grid/footprint cells have dedicated inspector authoring | **Ready** — typed `can_reach` and `step_toward` calls delegate to the shared world adapter | **Ready** — the Wisp follows the player around authored walls through Decay | Per-path policies, costs, and richer viewport wall painting |
| 3D content | **Partial** — cube primitive, depth, cameras, textured mesh foundation | **Partial** — preview, picking, and transform gizmos; no import, material authoring, or light | **Missing** | **Missing** | glTF/material/light path, then one 3D prop in Gather |
| Audio | **Ready** — WAV/Ogg/MP3 assets, silent/native/browser backends, scene source and playback lifecycle | **Partial** — the project browser lists audio files and `sindri.audio.source` is editable through the generic component inspector; no clip preview, and nothing gathers the audio a scene names | **Ready** — typed play/loop/pause/resume/stop intent without device I/O | **Ready** — background loop plus pickup and victory sounds on the shared host path | A `referenced_audio` gather so hosts load what a scene names, editor clip preview, per-voice/script handles, and buses/spatial audio |
| 2D physics and collision | **Ready** — masked Rapier2D runtime, body kinds, fixed-step stepping, masks, sensors/events, validation, velocity and impulse; `ScenePhysics2d` keeps the simulation in step with what a scene authors, including entities spawned mid-run | **Partial** — both components are registered with engine-accepted defaults and editable through the generic component inspector, and editor Play steps physics; no collider outlines or shape handles | **Ready** — typed velocity, impulse, and the four collision/sensor event queries, answered per entity as `Array<Entity>` | **N/A** — Gather has no collision to add; Orbital Last Stand exercises distinct player, enemy, hostile-shot, pickup and effect masks continuously | Collider gizmos in the Scene view, then ray/overlap queries when something needs one |
| 3D physics and collision | **Missing** — Sindri-owned 3D body/collider data model exists, but there is no 3D runtime or exercised behavior yet | **Missing** | **Missing** | **Missing** | Implement and exercise the parallel 3D runtime only after the 2D feature track is proven end to end |
| Projects and export | **Partial** — browser build exists, `sindri.toml` is editor metadata that nothing in the runtime reads, and there is no product export pipeline | **Partial** — a project is a directory holding `sindri.toml`; the welcome window lists, creates and opens one, and the browser roots at it. Still one scene at a time, and no project settings surface | **N/A** | **Partial** — Gather carries a manifest and opens as a project; the Pages host is still hand-built | Multiple scenes, a settings surface, a runtime that reads the manifest, CLI, native/web export pipeline |

## How a new feature moves through the table

A feature may land in stages, but each stage must say what remains. The usual
order is runtime model and tests, editor authoring, Decay access where gameplay
needs it, and a Gather use that applies real pressure. That is an order of
dependencies, not permission to forget later columns: the row remains partial
until the relevant counterparts exist.

Some features legitimately have no Decay surface. Serialization internals and
editor layout are examples. Mark those **N/A** rather than **Ready**, because a
green cell must mean something was implemented and exercised.
