# Agent guidance migration

Repository-wide coding-agent instructions now live in `AGENTS.md` so Codex,
Claude, ChatGPT, and other agents share one canonical set of project rules.
`CLAUDE.md` is intentionally only a pointer plus a place for future
Claude-specific exceptions.

The shared guidance includes the companion-game acceptance rule: relevant
runtime/editor/script gameplay capabilities should evolve Gather in the same
feature track and use the game as an integration proof.
