format_version = 1

[project]
name = "Causeway"
main_scene = "assets/causeway.scene.json"

# What the scene does not name. A script plays these by name at run time, and
# no walk of a scene can see a string inside a program.
[assets]
include = ["audio/victory.wav", "ui/causeway.weave"]
